import React, { useState, useEffect } from 'react';




// const sToP = (state) => {
// 	return {
// 		// loggedin: state.manageLogin.loggedin,
// 		// show: state.manageNavBar.show,
// 		// room: state.manageNavBar.room
// 	};
// };

// const dToP = (dispatch) => ({
// 	// login: (data) => dispatch({ type: 'LOGIN', payload: data }),
// 	// logout: () => dispatch({ type: 'LOGOUT' }),
// 	// toggle: () => dispatch({ type: 'TOGGLE' }),
// 	// addAllCelebs: (data) => dispatch({ type: 'ADD_ALL_PATIENTS', payload: data })
// });

// export default withRouter(connect(sToP, dToP)(Home));